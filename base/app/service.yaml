apiVersion: v1
kind: Service
metadata:
  name: argocd-backend
  namespace: argocd
  labels:
    app: backend
spec:
  type: ClusterIP
  selector:
    app: backend
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
