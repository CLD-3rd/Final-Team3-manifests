# Helm 차트 메타데이터 (릴리스/의존성 관리용)
apiVersion: v2
name: k6-test                                   # 차트 이름 (앱 이름과 달라도 됨)
description: Helm chart for running k6 load tests in EKS
type: application
version: 1.0.0                                   # 차트 버전(내부 관리용)
appVersion: "0.48.0"                              # k6 컨테이너 버전 표시용(권장)
